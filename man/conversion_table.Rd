% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/results.R
\name{conversion_table}
\alias{conversion_table}
\title{Wide, report-ready conversion table}
\usage{
conversion_table(eq, from = NULL, to = NULL)
}
\arguments{
\item{eq}{An `equate_recipe` that has been through [run_equating()].}

\item{from, to}{Optional form names selecting the pairing. If omitted and only
one pairing exists it is used; otherwise the first is used with a message.}
}
\value{
A data frame with a `x_score` column and one column per method. The
  selected `from`/`to` are attached as attributes.
}
\description{
Reshapes the tidy [conversions()] into a raw-score-by-method table for a
single form pairing: a `x_score` column followed by one column of equated
scores per method. This is the layout you typically paste into a report.
}
