% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plots.R
\name{plot_equating_comparison}
\alias{plot_equating_comparison}
\title{Plot a Comparison of Multiple Equating Functions}
\usage{
plot_equating_comparison(
  results_list,
  title = NULL,
  relative = FALSE,
  gg = FALSE
)
}
\arguments{
\item{results_list}{A named list where each element is the result object
for a single equating method. Each element must contain `x_score` and
`equivalents`.}

\item{title}{A character string for the main title of the plot.}

\item{relative}{A logical value. If `TRUE`, the plot shows the difference
between the equated score and the identity line (y - x). If `FALSE`
(the default), it shows the standard equating function.}

\item{gg}{A logical value. If `TRUE`, the function returns a ggplot2 object.
If `FALSE` (the default), it creates a base R plot.}
}
\value{
If `gg = FALSE`, the function is called for its side effect of creating a plot.
  If `gg = TRUE`, the function returns a ggplot object.
}
\description{
This function takes a list of equating results and overlays their equating
functions on a single plot for easy visual comparison. It can generate
either a standard plot or a plot of the difference from the identity line.
}
\author{
Google's Gemini
}
