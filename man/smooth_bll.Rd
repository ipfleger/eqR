% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/LogLinear.R
\name{smooth_bll}
\alias{smooth_bll}
\title{Perform Bivariate Log-Linear Smoothing}
\usage{
smooth_bll(
  n,
  nsu,
  minu,
  incu,
  nsv,
  minv,
  incv,
  nct,
  cu,
  cv,
  cuv,
  cpm,
  scale = FALSE,
  crit = 1e-05,
  max_nit = 100
)
}
\arguments{
\item{n}{An integer, the total number of examinees.}

\item{nsu}{An integer, the number of score categories for the first variable (u).}

\item{minu}{A numeric value, the minimum score for variable u.}

\item{incu}{A numeric value, the increment for variable u.}

\item{nsv}{An integer, the number of score categories for the second variable (v).}

\item{minv}{A numeric value, the minimum score for variable v.}

\item{incv}{A numeric value, the increment for variable v.}

\item{nct}{A numeric vector representing the flattened bivariate frequency
table. The vector should be created by reading the table row-by-row
(i.e., `as.vector(t(bivariate_matrix))`).}

\item{cu}{An integer, the degree of the polynomial for variable u.}

\item{cv}{An integer, the degree of the polynomial for variable v.}

\item{cuv}{An integer, the number of cross-product terms in the model.}

\item{cpm}{A matrix with `cuv` rows and 2 columns, where each row specifies
the powers `(i, j)` for a cross-product term `u^i * v^j`.}

\item{scale}{A logical value. If TRUE, the columns of the design matrix
are centered and scaled.}

\item{crit}{A numeric value, the convergence criterion for the moments.}
}
\value{
A list containing the full results of the smoothing process,
  including the Beta coefficients, fitted frequencies (`mct`), number of
  iterations, chi-square statistics, and moments. This list is the
  `bivar` object required by the `equate_cll` function.
}
\description{
This is a high-level wrapper function that performs bivariate log-linear
smoothing on a two-way frequency distribution. It creates the design matrix,
runs the iterative fitting algorithm, and returns the results. This is an R
translation of `Smooth_BLL`.
}
\seealso{
\code{\link{iteration}}, \code{\link{design_matrix}}, \code{\link{equate_cll}}
}
